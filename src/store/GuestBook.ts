import GuestBookModel from '../interfaces/GuestBookModel';

const GuestBook: GuestBookModel = {
  entries: [{ name: 'Rifqi', content: 'First Content', submitted: new Date() }],
};

export default GuestBook;
